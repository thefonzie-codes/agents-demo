version: '3.8'

services:
  travel-agent:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - FLASK_APP=backend/app.py
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
